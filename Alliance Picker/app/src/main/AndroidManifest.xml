<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elliott.alliancepicker" >

    <application
        android:name=".InstantizedVars"
        android:allowBackup="true"
        android:icon="@drawable/scout_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".QuestionActivity"
            android:label="@string/title_activity_question"
            android:icon="@mipmap/ic_launcher">
        </activity>
        <activity
            android:name=".InfoActivity"
            android:label="@string/title_activity_info"
            android:icon="@mipmap/ic_launcher">
        </activity>
        <activity
            android:name=".Tutorial"
            android:label="Tutorial"
            android:icon="@mipmap/ic_launcher">
        </activity>
        <activity
            android:name=".CustomizeActivity"
            android:label="@string/title_activity_customize"
            android:icon="@mipmap/ic_launcher">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.elliott.alliancepicker.MainActivity" />
        </activity>
    </application>

</manifest>
